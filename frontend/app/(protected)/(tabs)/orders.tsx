import { Text, View } from "react-native";

export default function Orders(){


    return(
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center', gap: 20}}>
            <Text>Não há nenhum pedido...</Text>
        </View>
    )
}